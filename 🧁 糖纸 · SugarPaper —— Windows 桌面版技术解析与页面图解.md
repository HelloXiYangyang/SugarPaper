# 🧁 糖纸 · SugarPaper —— Windows 桌面版技术解析与页面图解

> 基于仓库当前代码（v0.32.0，2026-08-08）整理；覆盖技术栈、依赖库、六大页面 ASCII 图解、页面逻辑、桌面端特有实现与页面风格提炼。

---

## 1. 技术栈总览

| 层 | 选型 | 说明 |
| :--- | :--- | :--- |
| UI 框架 | Flutter 3.44.8（Dart 3.12.2），Material 3 | 与安卓版共享 `app/lib/` 全部核心代码，一套代码多端 |
| 原生壳 | C++ Win32（`windows/runner/`：main.cpp / flutter_window / win32_window） | 窗口创建、消息循环、DPI、图标资源；VS2022 + Windows 10/11 SDK |
| 构建 | CMake + MSVC，`flutter build windows --release` | `/utf-8` 编译选项修复中文注释 C4819；Debug/Profile/Release 三配置 |
| 状态管理 | flutter_riverpod 2.6（`ChangeNotifierProvider`） | 全局 `AppState`（界面状态）+ `AppStore`（数据层） |
| 本地存储 | JSON 文件（`path_provider` 定位应用文档目录） | 离线优先；备份格式与 Web 版完全兼容，可互导 |
| 图表 | 全部自绘 CustomPainter | 进度环 / 柱状 / 饼图 / 折线 / 报告卡 / 番茄钟圆环，零图表依赖 |
| 图标 | 自绘 24×24 描边 SVG path（`svg_path.dart` + CustomPainter） | path 数据与网页版 `icons.js` 一致 |
| 通知 | flutter_local_notifications（Windows toast） | AUMID + GUID 激活回调，无需申请权限 |
| 音频 | audioplayers + 纯 Dart WAV 合成（`noise_gen.dart`） | 9 种白噪音场景循环播放 + 自定义音频 + 双音轨混音 |
| 语音 | speech_to_text（zh-CN） | 便签 / 导入弹窗语音转文字，需 RECORD_AUDIO |
| 同步 | dart:io WebSocket（Nostr 协议）+ flutter_webrtc（DataChannel P2P） | kind 19322 加密快照同步；kind 19323 信令 + WebRTC 直连 |
| 加密 | cryptography（Pbkdf2 / Ed25519 / AesGcm / Sha256） | 12 词助记词身份、端到端加密、签名验签、SHA-256 校验 |
| 更新 | dio 流式下载 + SHA-256 校验 + `Process` 拉起 Inno Setup | 弹窗内前台进度条，`/VERYSILENT /NORESTART` 静默安装 |
| 打包 | Inno Setup 6（ISCC）+ PowerShell 脚本 + GitHub Actions | 绿色版 zip + 安装包 exe；打 tag 自动构建并回填 latest.json |

---

## 2. 依赖库清单

| 包 | 版本 | 用途 |
| :--- | :--- | :--- |
| flutter / flutter_localizations | SDK | 框架 + 中文本地化（zh-CN） |
| flutter_riverpod | ^2.5.1 | 全局状态管理 |
| path_provider | ^2.1.3 | 定位应用文档目录（数据文件 / WAV / 更新包） |
| intl | ^0.20.2 | 日期与国际化格式化 |
| image_picker | ^1.1.2 | 头像 / 便签图片选择 |
| image | ^4.2.0 | 图片压缩（最长边 512px，JPEG 质量 82） |
| audioplayers | ^6.1.0 | 白噪音 / 自定义声音播放（双音轨） |
| flutter_local_notifications | ^19.0.0 | Android 通知 + Windows toast |
| speech_to_text | ^7.0.0 | 语音速记（zh-CN） |
| cryptography | ^2.7.0 | PBKDF2 / Ed25519 / AES-256-GCM / SHA-256 |
| flutter_webrtc | ^0.12.6 | 设备间 P2P 直连（DataChannel 互传加密快照） |
| file_picker | 10.3.3 | .txt/.md 导入、自定义音频上传 |
| share_plus | 10.1.2 | 统计报告 PNG / 教师作业文本分享导出 |
| package_info_plus | ^9.0.1 | 读取当前版本号 / build 号 |
| dio | ^5.11.0 | 更新包流式下载 + 进度回调 |
| open_filex | ^4.7.0 | Android 调起 APK 安装器 |

Windows 侧原生 / 构建工具：Win32 API（`win32_window`、`flutter_window`）、CMake、Inno Setup 6（`sugarpaper.iss`）、PowerShell（`build_windows.ps1` / `build_installer_windows.ps1`）、GitHub Actions（`windows-latest` + choco innosetup）。

---

## 3. 启动流程与生命周期

```text
main()
  │
  ├─ ① WidgetsFlutterBinding.ensureInitialized()
  ├─ ② DeviceCapabilities.init()          ← MethodChannel 读系统 API level（缓存）
  ├─ ③ AppStore().load()                  ← 读 JSON 数据文件（任务/便签/设置/账号）
  ├─ ④ appLegalStore.load()               ← 用户协议 / 隐私政策同意状态
  ├─ ⑤ appUpdatePrefs.load()              ← 「忽略此版本」偏好
  ├─ ⑥ ReminderService.init()             ← 通知插件初始化（Android + Windows toast）
  │      └─ 若开启通知 → requestPermission()
  ├─ ⑦ store.onPersist = 提醒检查 + 自动同步
  │      ├─ 每次数据变更 → ReminderService.checkAll()
  │      └─ 开启自动同步且有账号 → 5 秒防抖后 syncOnce()
  ├─ ⑧ runApp(ProviderScope(overrides: appStoreProvider))
  │
  └─ 首帧后：
       ├─ 帧率采样（1.8s / 90 帧中位数 → 60/90/120/144 档）
       └─ 启动 6 秒后静默检查更新（忽略已忽略版本 → 弹更新对话框）
```

入口页决策：`appLegalStore.isAgreed == true` → `AppShell`；否则 → `LegalGatePage`（强制阅读并勾选两份协议后才能进入）。

---

## 4. 全局状态与数据流

```text
AppStore（数据层，JSON 持久化）
  ├─ tasks / notes / focusSessions / subjects / settings / account
  └─ 方法：addTask / toggleComplete / importTasks / addNote / updateSettings …
        │  每次变更 → notify() + persist()（自动触发提醒与防抖同步）
        ▼
AppState（界面状态，Riverpod ChangeNotifier）
  ├─ view       当前 Tab（home/notes/focus/calendar/stats/settings）
  ├─ query      搜索词（180ms 防抖）
  ├─ subject    科目筛选（全部/某科）
  ├─ priority   优先级筛选（全部/高/中/低）
  ├─ calMode / calSelected / statsRange   日历与统计视图状态
  └─ detectedFps 实测帧率（auto 档跟随）
        │  派生数据
        ├─ filteredTasks      ← 科目 + 优先级 + 搜索 三重过滤
        ├─ groupedActiveTasks ← 五档截止分级：已逾期/今天/明天/本周/长期
        ├─ stats              ← StatsEngine.compute（进度/科目分布/欠账/趋势）
        └─ activeNotes        ← 置顶优先、未归档在前
```

所有页面都是 `ConsumerWidget`：`ref.watch(appStateProvider)` 订阅状态，任何数据变化自动重建相关视图。

---

## 5. 应用外壳（AppShell）

六个一级页面：首页 / 便签 / 专注 / 日历 / 统计 / 我的。断点 ≥840px 走桌面侧边栏，否则底部导航。

**宽屏（≥840px，Windows 桌面默认形态）：**

```text
┌──────────────┬───────────────────────────────────────────────┐
│ 侧边栏 232px │  当前页面内容（AnimatedSwitcher 页面过渡）      │
│              │                                               │
│ 🍬 糖纸·Sugar │   方向感知滑动 + 淡入淡出 + 缩放               │
│ ──────────── │                                               │
│ 首页          │                                               │
│ 便签          │                                               │
│ 专注          │                                               │
│ 日历          │                                               │
│ 统计          │                                               │
│ 我的          │                                               │
│ ──────────── │                                               │
│ 科目          │                                               │
│ ● 全部    12  │                                               │
│ ● 语文     3  │                                               │
│ ● 数学     2  │                                               │
└──────────────┴───────────────────────────────────────────────┘
```

**窄屏（<840px）：**

```text
┌───────────────────────────────────────────────┐
│              当前页面内容                       │
├───────────────────────────────────────────────┤
│ [首页] [便签] [专注] [日历] [统计] [我的]  64px │
└───────────────────────────────────────────────┘
```

逻辑要点：侧边栏点击科目 → 切回首页并设置该科目筛选；底部导航图标激活态放大 1.12 倍（easeOutBack 回弹）；页面切换按前进/后退方向横向滑动，移动端改为上浮。

---

## 6. 六大页面图解与逻辑

### 6.1 首页（HomePage）

```text
┌────────────────────────────────────────────────┐
│ [🍬Logo] 糖纸                 [＋导入] [头像]   │
│ ▓▓▓▓▓▓▓▓░░░░  进度 60% · 3/5                  │
│ ⚠ 距上次备份已超7天  [去备份]                  │
│ ┌ 今日概览 ──────────────────────────┐        │
│ │ 今日概览                  🔥3天连胜│        │
│ │  ╭─╮ 72%   完成作业  2/3   ▓▓▓░░  │        │
│ │  │XP│       今日 XP  40/50 ▓▓▓░░  │        │
│ │  ╰─╯       专注分钟  25           │        │
│ └────────────────────────────────────┘        │
│ [🔍 搜索作业...       ] [优先级 ▾]             │
│ (全部)(语文)(数学)(英语)...  ← 科目Chip横滚    │
│ ── 已逾期 1 ──────────────────────            │
│ ● 语文 背《昆虫记》讲义  [高] ✓ ✎ 🗑 ⏱ ↑ ↓   │
│ ── 今天 1 ──  ── 明天 1 ──  ── 本周 1 ──      │
│ ── 已完成 1 ────────────────────              │
└────────────────────────────────────────────────┘
```

页面逻辑：

- 顶栏：Logo + 导入按钮（`showImportSheet`）+ 头像（点击进设置）；下方流光进度条显示完成百分比。
- 数据安全网：超 7 天未备份显示黄色横幅，一键去设置页导出。
- 今日概览卡（v0.32.0 激励体系）：XP 进度环 + 完成作业 / 今日 XP / 专注分钟三条目标行（弹性进度条）+ 连胜火焰。
- 过滤：搜索框 180ms 防抖；优先级下拉；科目 Chip 横向滚动（带数量角标）。
- 任务列表：五档截止分级（已逾期/今天/明天/本周/长期，逾期置顶标红，组内优先级降序），每组 GroupHeader + 任务卡。
- 任务卡操作：完成/撤销（带动画）、编辑、删除（确认弹窗）、开始专注（推入专注页）、家长确认、上移/下移排序。
- 完成作业 → `RewardsEngine`：XP 飘字浮层（`+XP · 欠账补完·双倍`）→ 徽章解锁弹窗（缩放+辉光）→ 全部完成时撒花纸屑动画（1.8s）。
- 空状态：无任务 → 欢迎卡（导入 / 载入示例）；筛选无结果 → 「没有匹配的作业」。

### 6.2 便签页（NotesPage）

```text
┌────────────────────────────────────────────────┐
│ [list] 便签                          [＋新建]   │
│ ┌ 便签卡 ──────────────────────────────┐       │
│ │ ●(色点) 标题                [📌] [✎] │       │
│ │ 内容预览（最多3行）…                 │       │
│ │ [🖼] [🖼] [🖼] [🖼]  ← 图片缩略图      │       │
│ │ [置顶] [归档] [转作业]        [🗑]    │       │
│ └──────────────────────────────────────┘       │
│ ...                                           │
│ ── 已归档 2 ──                                 │
└────────────────────────────────────────────────┘

（新建/编辑便签 = 居中弹窗）
┌ 新建便签 ──────────────────────────┐
│ 标题（可选）[____________]          │
│ [编辑] [预览]                       │
│ 内容…（Markdown / - [ ] 待办 / 🎤）│
│ ○○○○○○○○   ← 8 色马卡龙色点       │
│ 🔔 设置提醒 [添加]                  │
│ [添加图片（最多4张）]               │
│              [取消] [创建]          │
└────────────────────────────────────┘
```

页面逻辑：

- 列表排序：置顶优先、未归档在前、按更新时间倒序；归档分组展示，可恢复。
- 卡片操作：点击编辑；置顶/取消置顶；归档/恢复；删除（确认）；「转作业」→ 解析标题+内容 → 预览解析结果 → 追加导入（勾选的待办直接以已完成导入）。
- 编辑器：标题 + 内容（Markdown 编辑/预览切换，`MdRender` 渲染）；语音输入按钮（zh-CN）；8 色标签；提醒时间（日期+时间选择器）；最多 4 张图片（自动压缩 512px，可删除）。

### 6.3 专注页（FocusPage）

```text
┌────────────────────────────────────────────────┐
│ [bolt] 专注      今日已专注25分·1番茄 [25分钟]  │
│ ┌ 番茄钟 ────────────────────────────┐         │
│ │        ╭──────────╮               │         │
│ │        │  25:00   │ ← 渐变圆环进度 │         │
│ │        │ 自由专注  │               │         │
│ │        ╰──────────╯               │         │
│ │   [番茄][短休][长休][自由]         │         │
│ │    (⟲)    ( ▶ 76px )    (✓)      │         │
│ └────────────────────────────────────┘         │
│ ┌ 环境与噪音 ────────────────────────┐         │
│ │ [粉红][白噪][棕噪][雨][篝火][图书]  │         │
│ │ [森林][海浪][自定义] ← 渐变氛围卡   │         │
│ └────────────────────────────────────┘         │
│ ┌ 声音与氛围 ────────────────────────┐         │
│ │ 呼吸引导                [开关]     │         │
│ │ 主场景音量 ──────○──── 60%         │         │
│ │ 叠加声音：[无][雨][篝火]…          │         │
│ │ 叠加音量 ──○──── 40%               │         │
│ │ [上传自定义声音（≤2MB）]           │         │
│ └────────────────────────────────────┘         │
└────────────────────────────────────────────────┘
```

页面逻辑：

- 四档预设（番茄 25 / 短休 5 / 长休 15 / 自由不限时）；1 秒 Timer 倒计时，到 0 自动完成。
- 完成番茄 → 记录 `FocusSession`（关联任务/时长/场景/番茄数）→ 入统计与日历角标。
- 场景：9 种内置（粉红/白/棕噪音、雨、篝火、图书馆、森林、海浪、自定义）+ 静音；选中即切换页面背景氛围渐变并播放对应 WAV；场景卡片选中态高亮描边 + 光晕。
- 混音：主场景音量 + 叠加声音（Noisli 式双音轨），各自独立音量；自定义音频 ≤2MB 上传/更换/删除。
- 呼吸引导：8 秒正反循环光晕动画，可开关。
- 嵌入模式（作为 Tab）：顶部显示「今日已专注 X 分钟」；从任务卡进入时显示返回键 + 关联任务标题，完成后联动。

### 6.4 日历页（CalendarPage）

```text
┌────────────────────────────────────────────────┐
│ [日历] 2026年 8月  [‹] [今天] [›] [月|周] [导出]│
│  一  二  三  四  五  六  日                     │
│ ┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐                  │
│ │  ││  ││  ││ 1││ 2││ 3││ 4│                  │
│ │  ││  ││  ││ ●││  ││考 ││ ●│  ← 任务点/考/分  │
│ └──┘└──┘└──┘└──┘└──┘└──┘└──┘                  │
│ ...（选中日高亮主题色，今日粉底）                │
│ ── 8月4日 的任务（未完成1·已完成1）   [＋添加]  │
│ [✓] 语文 背《昆虫记》   [高]                    │
│ [✓] 数学 试卷一张       [中]                    │
└────────────────────────────────────────────────┘
```

页面逻辑：

- 月视图（周一起始）：日期格显示任务点（绿=已完成/灰=未完成，超 3 个折叠计数）；考试便签显示黄色「考」角标；当日专注分钟角标；今天粉底、选中主题色高亮。
- 周视图：7 列并排，每列当日任务（完成划线），今天粉底。
- 下方当日任务列表：按优先级排序，可勾选完成/撤销、查看优先级，支持「添加任务」（预填该日期）。
- 导出：当月有任务的日期生成文本清单，复制到剪贴板。
- 工具栏：上月/下月、回到今天、月/周切换分段控件。

### 6.5 统计页（StatsPage）

```text
┌────────────────────────────────────────────────┐
│ [chart] 统计              [今日|本周|本月|全部] │
│                        [导出]                  │
│ ┌ 整体进度 ──────────────────────────┐         │
│ │  ╭─╮  总任务 12 · 已完成 8(67%)   │         │
│ │  │◔│  进行中 4 · 连续完成 3 天    │         │
│ │  ╰─╯  本周均率 75% · 准时率 80%   │         │
│ └────────────────────────────────────┘         │
│ ┌ 科目分布（饼图）──────────────────┐         │
│ │ ╭─╮  语文 25% · 数学 33% · …     │         │
│ └────────────────────────────────────┘         │
│ ┌ 每日完成趋势（柱状图）────────────┐         │
│ ┌ 历史完成趋势（折线图·最近8周）────┐         │
│ ┌ 高频未完成科目 Top3 ─────────────┐         │
│ ┌ 科目欠账排行（考前复习）─────────┐         │
│ ┌ 激励（XP）今日/本周/最佳单日 ────┐         │
│ ┌ 专注统计 今日/本周/番茄数 ───────┐         │
└────────────────────────────────────────────────┘
```

页面逻辑：

- 时间范围（今日/本周/本月/全部）影响整体进度卡与底部「X一览」，图表数据由 `StatsEngine.compute` 一次性派生。
- 图表全部自绘：进度环、饼图（转圈加载→扇区绽放）、柱状图（自底生长）、折线图（描边 1.1s）。
- 洞察卡：高频未完成科目 Top3、科目欠账排行（未完成+逾期天数加权，逾期标红）、准时完成率（完成日≤截止日）。
- 激励卡：今日/本周 XP、最佳单日、连胜；每日目标进度。
- 专注卡：今日/本周专注分钟、累计完成番茄数。
- 导出：底部弹层二选一 —— 复制文本报告（剪贴板）或导出 PNG（`ReportCardPainter` 640×880 自绘报告卡 → 预览 → 保存分享）。

### 6.6 设置页 / 我的（SettingsPage）

```text
┌────────────────────────────────────────────────┐
│ ┌ 设备卡 ────────────────────────────┐         │
│ │ [头像] 我的设备       [换头像][改名]│         │
│ │ 离线优先 · 本地存储 · 数据大小 1.2KB│         │
│ └────────────────────────────────────┘         │
│ ┌ 个性化 ────────────────────────────┐         │
│ │ 帧率模式        [自动|60|120]      │         │
│ │ 流畅度自测      [测一测]            │         │
│ │ 通知提醒        [开关]（Android8+）│         │
│ │ 互联网模式      [开关]              │         │
│ │ 主题            (⊙)(○)(○)(○)(○)(○)(○)│       │
│ │ 提醒时间        前一晚20:00 [调整]  │         │
│ └────────────────────────────────────┘         │
│ ┌ 激励与成就：每日XP/完成目标 + 徽章墙┐         │
│ ┌ 家庭模式：成员档案切换/添加 ───────┐         │
│ ┌ 数据管理：导出/导入/示例/清空 ─────┐         │
│ ┌ 教师模式：作业文本生成器 [打开] ───┐         │
│ ┌ 账号与同步（AccountCard）──────────┐         │
│ ┌ 科目管理：16科列表 + 添加 ─────────┐         │
│ ┌ 更新：检查更新（GitHub Pages）─────┐         │
│ ┌ 法律与隐私：用户协议/隐私政策 ─────┐         │
│ ┌ 关于 ──────────────────────────────┐         │
│ │  [🍬App图标]                      │         │
│ │  糖纸 · SugarPaper v0.32.0（安卓版）│       │
│ │  让作业管理像糖果一样甜美简单       │         │
│ └────────────────────────────────────┘         │
└────────────────────────────────────────────────┘
```

页面逻辑：

- 设备卡：头像（默认喜羊羊图 / 相册上传压缩 256×256 / 恢复默认）、改名、数据大小估算。
- 个性化：帧率模式（自动/60/120，Android 11+ 才显示 120）、流畅度自测（Toast 显示实际帧率）、通知提醒、互联网模式、七套平行主题、提醒时间自定义。
- 激励与成就：每日 XP 目标（30/50/80/100）与完成目标（2/3/5/8）分段选择 + 12 枚徽章墙（解锁状态随加密快照同步）。
- 家庭模式：成员档案（昵称）一键切换代管、添加/移除。
- 数据管理：导出 JSON 到剪贴板、导入恢复、载入示例、清空全部（确认弹窗，红色警示）。
- 教师模式：按标准格式生成作业文本，复制/导出 .txt 发班级群。
- 账号与同步：12 词助记词创建/恢复/备份、中继配置、自动同步开关、在线直连状态。
- 科目管理：16 科默认清单，增删改/启停/配色。
- 更新：`UpdateService.checkForUpdate()` 读 `updates/latest.json` 比较 build 号 → 弹更新对话框（忽略此版本/稍后再说/立即升级）。
- 法律与隐私：内置协议 Markdown 渲染查看；关于卡片显示版本号。

---

## 7. 关键弹窗与浮层

| 弹窗 | 触发 | 交互逻辑 |
| :--- | :--- | :--- |
| 导入作业 | 首页右上角 ＋ | 粘贴文本 / 从 .txt/.md 导入 / 语音输入 → 解析器预览 → 覆盖 / 追加 |
| 编辑作业 | 任务卡 ✎ / 日历添加 | 科目/标题/副标题/截止日期快捷规划（今天/明天/本周/长期）/优先级/图片附件/家长确认 |
| 新建/编辑便签 | 便签页按钮/点击卡片 | 见 6.2 编辑器 |
| 更新对话框 | 启动 6s 后 / 设置页检查 | 版本号 + 更新说明 + 忽略此版本 / 稍后再说 / 立即升级；Windows 前台进度条 → 校验 → 静默安装 → 退出重启 |
| 徽章解锁 | 完成作业触发 | 缩放 + 辉光动画，多枚时提示剩余数量 |
| 教师模式 | 设置页打开 | 选科目填条目 → 生成标准文本 → 复制/下载 |
| 导出统计 | 统计页导出 | 底部弹层：复制文本 / 导出 PNG 预览 |

---

## 8. Windows 桌面端特有实现

```text
┌─────────────────────────────────────────────────────┐
│ 窗口（Win32 + FlutterViewController）                │
│  • 标题「糖纸 · SugarPaper」 · 默认 1280×800          │
│  • 最小 900×640（WM_GETMINMAXINFO）                  │
│  • 应用图标 app_icon.ico（Runner.rc 资源，进 exe）    │
│  • WM_FONTCHANGE → ReloadSystemFonts（字体热更新）    │
├─────────────────────────────────────────────────────┤
│ 桌面侧边栏（≥840px）：品牌 + 六导航 + 科目快捷筛选     │
├─────────────────────────────────────────────────────┤
│ 系统通知：flutter_local_notifications → Windows toast │
│   AUMID: HelloXiYangyang.SugarPaper                  │
├─────────────────────────────────────────────────────┤
│ 更新链路：dio 前台下载 → SHA-256 流式校验 →            │
│   Process.start(setup.exe, /VERYSILENT /NORESTART)   │
│   → exit(0)（安装器独立进程接管）                     │
├─────────────────────────────────────────────────────┤
│ 打包：build_windows.ps1（绿色版 zip）+                │
│   build_installer_windows.ps1（Inno Setup 安装包）    │
│   GitHub Actions windows job：choco innosetup → ISCC │
│   → SugarPaper-vX.Y.Z-windows-x64.exe + SHA-256      │
└─────────────────────────────────────────────────────┘
```

注意事项：release（AOT）构建要求源码路径不含中文等非 ASCII 字符 —— 本仓库在中文路径下需用纯英文目录联接（如 `D:\SugarPaperDev`）或直接走 CI。

---

## 9. 页面风格提炼

1. **马卡龙低饱和 Token 体系**：全局 `SugarThemeData` 定义 bg/surface/text/border + 粉/薄荷/天空/薰衣草/桃/黄 六色系（各配 Strong/Soft），七套平行主题（经典白 + 5 套马卡龙 + 暗色），主题化图标配色（iconMain/iconAccent/iconSoft）。
2. **卡片化布局**：所有页面 = ListView + 圆角卡片（Radius 14–22）；卡片统一白底/主题底 + 细边框 + 柔和阴影（0xFF7A5C68 alpha 6–9%）。
3. **图标即语言**：全界面自绘 24×24 描边 SVG 图标，卡片标题一律「小圆角图标块 + 加粗标题」，语义清晰。
4. **轻量动效**：页面方向感知滑动过渡、卡片错落入场（Reveal）、开关弹簧回弹、进度环/柱状/折线绘制动画、按压缩放反馈；动画时长集中在 180–700ms，全程尊重系统减弱动效。
5. **统一交互控件**：SugarButton（主按钮主题渐变/危险红）、SugarSwitch、PressableScale（按压缩放）、SugarToast（悬浮圆角）、EmptyState（图标+标题+副标题+按钮）。
6. **空状态友好**：每个页面都有专属空状态引导（欢迎导入、无匹配、无便签、无任务、无欠账）。
7. **状态可视化**：首页进度条、今日概览环、统计图表、日历任务点/角标、设置徽章墙 —— 数据洞察贯穿全端。

---

## 10. 页面逻辑通则（提炼）

- **单向数据流**：页面事件 → `AppStore` 修改数据 → `notify()` → Riverpod 通知所有订阅页面重建；界面筛选状态（搜索/科目/优先级/日期/范围）全部收敛在 `AppState`。
- **派生优先**：列表分组（五档截止分级）、统计报告、科目计数、进度百分比都是纯函数派生（`AppState` getter / `StatsEngine` / `RewardsEngine`），不冗余存储。
- **反馈闭环**：每个写操作都有即时反馈 —— 完成动画、XP 飘字、徽章解锁、Toast、撒花；每个危险操作都有确认弹窗。
- **离线优先**：数据只存本地 JSON，同步/更新/网络全部为可选增强；账号=助记词密钥，端到端加密。
- **跨端一致**：页面结构、图标、动画、断点与网页版对齐；桌面版额外提供侧边栏、系统通知、窗口管理与安装器更新。
