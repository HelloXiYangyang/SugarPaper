# 🧁 糖纸 · SugarPaper —— S18 需求文档：激励体系 · 好友直连 · 桌面组件 · 风格方案

> 版本：v1.0.0（2026-08-07）
> 适用：网页版 / 安卓版 / 后续 Windows 桌面版；按《版本号更新体系》同步推进（v0.32.0 起）
> 原则：离线优先 · 零服务器 · 端到端加密 · 不引入公开社交与全球排行榜

---

## 0. 总览

本需求文档汇总四个开发模块 + 一个待定风格方案，均**保留现有全部功能**，只做增量：

| 模块 | 内容 | 建议版本 |
| :--- | :--- | :--- |
| 模块一 | 多邻国式个人激励体系（连胜 / XP / 徽章 / 欠账复习 / 目标 / 完美周 / 家庭排行） | v0.32.0 |
| 模块二 | 好友直连与端到端加密通信（去中心化，无中心服务器） | v0.33.0 |
| 模块三 | Windows 桌面端增强（托盘 / 系统通知 / 全局快捷键 / 拖拽 OCR） | 待 Windows 环境 |
| 模块四 | 桌面组件（安卓 App Widget + Windows 悬浮迷你窗） | v0.34.0 |
| 模块五 | 第二套界面风格（**方案先行，暂不实施**） | 待风格方向确认 |

多版本同步开发规范（S12 起强制）对本需求全部模块适用：网页版与安卓版同版本同步开发、同步验收、同步文档；Windows 等平台在有构建环境后按同一规范补齐。

---

## 模块一：多邻国式个人激励体系（v0.32.0）

### 1.1 设计目标

借鉴 Duolingo 的上瘾机制，将作业管理游戏化，但**全部为个人激励，不引入公开社交/全球排行榜**（README 产品红线）。所有数据仅存本机，随现有加密快照同步。

### 1.2 连胜 Streak

- 定义：连续 N 天每天至少完成 1 项作业（完成 = `isCompleted` 且 `completedAt` 为当天）。
- 展示：统计页/首页显示火焰图标 + 连续天数；连胜日历（本月每天打勾/火焰标记）。
- 提醒：晚上 21:00 若当日已完成 0 项，推送「再完成 1 项，保住 X 天连胜」；连胜中断后显示「昨天中断了 X 天连胜」。
- 可选扩展：周末护盾（周六日自动冻结，不中断连胜；默认关闭）。

### 1.3 每日 XP 与目标

- 经验值规则（新增 `xp` 派生值，不破坏现有数据模型）：

| 任务类型 | XP |
| :--- | :--- |
| 打卡类 | 10 |
| 背诵类 | 15 |
| 书面类 | 20 |
| 高优先级（priority=2） | 额外 +50% |
| 欠账补完（逾期后完成） | 双倍（见 1.5） |

- 每日目标：设置页可设目标（默认 50 XP）；首页顶栏/统计页显示「今日 32/50 XP」进度环，完成时糖果雨庆祝（复用现有撒花）。
- 周报新增：本周总 XP、日均 XP、最佳日。

### 1.4 成就徽章墙

本地解锁、设置页「徽章墙」展示（图标 + 名称 + 解锁时间 + 未解锁置灰）：

- 初来乍到：完成第 1 项作业
- 十全十美：累计完成 10 项
- 百炼成钢：累计完成 100 项
- 三天打鱼：连续 3 天完成
- 一周连胜：连续 7 天完成
- 全勤王：连续 30 天完成
- 专注达人：累计 100 个番茄
- 夜猫子：累计 50 个番茄（21 点后完成）
- 准时侠：准时率 90%（完成日 ≤ 截止日）
- 逆袭者：一次欠账补完
- 满分周：一周内全部按时完成（完美周）
- 家人好帮手：家庭模式成员完成 20 项

### 1.5 欠账复习（借鉴 Duolingo 复习机制）

- 逾期未完成的作业自动进入「欠账清单」（首页新增分组置顶）。
- 完成欠账作业：XP 双倍 + 「逆袭者」徽章进度；欠账清单清空时庆祝。
- 欠账数据为派生视图，不改变任务存储。

### 1.6 每日/每周目标设定

- 可设：每日完成项数目标（默认 3）、每日专注分钟目标（默认 25）、每日 XP 目标（默认 50）。
- 展示：首页进度胶囊、专注页今日统计、统计页目标完成率。

### 1.7 家庭内部排行（不碰社交红线）

- 基于现有「家庭模式」成员档案，显示**本机家庭内部**本周完成项数 / XP 排行。
- 不上传、不公开、无全球榜；仅家人可见。

### 1.8 数据模型（建议）

在现有 `store.js` / `AppStore` 增加派生计算，不新增持久化字段；徽章解锁状态可存 `settings.achievements`（随加密快照同步）。

---

## 模块二：好友直连与端到端加密通信（v0.33.0）

### 2.1 设计目标

不同账号之间"加好友 + 加密通信 + 共享内容"，**完全去中心化**：无中心服务器、无我方运营的账号系统，复用现有 Nostr 中继 + WebRTC + 端到端加密基础设施。

### 2.2 身份与加好友

- 身份 = 现有 Ed25519 公钥（npub / hex），无需新账号体系。
- 添加方式：
  1. 扫码（好友二维码 = npub）
  2. 复制 / 分享 npub 文本或链接
  3. 手动粘贴公钥
- 好友列表（本地存储）：昵称、头像、公钥、添加时间、备注；可删除。
- 好友确认：A 添加 B 后，B 收到请求（加密消息），同意后互为好友；未确认的单向添加仅能发送「好友请求」。

### 2.3 端到端加密协议

- 密钥协商：Ed25519 私钥转换为 X25519，双方公钥经 X25519 派生共享密钥（`cryptography` 包支持 Curve25519）。
- 消息加密：AES-256-GCM（随机 nonce），载荷 JSON：`{ type, nonce, ciphertext, from, ts }`。
- 传输：Nostr 自定义事件（如 kind 20000–20009 段，避免与现有同步事件冲突），中继仅存储转发密文；双方在线时自动升级为 WebRTC DataChannel 直连。
- 签名：Ed25519 签名防篡改、防伪造发送者。

### 2.4 功能范围

1. 好友管理：列表 / 添加 / 删除 / 备注 / 请求确认
2. 加密消息：点对点文字消息 + 送达状态（本机已发送/已读可选）
3. 好友间共享：一键「分享作业」「分享便签」「分享周计划」→ 加密发送 → 对方收到可导入（复用现有导入逻辑）或收藏
4. 在线状态：WebRTC 直连时显示「在线」，离线消息暂存中继
5. 隐私控制：删除好友即停止接收；可关闭「允许陌生人请求」

### 2.5 安全与边界

- 中继可见：发送时间、公钥、密文大小（元数据），内容不可读（协议与隐私政策已声明）。
- 防骚扰：默认只接收已知公钥；请求需确认。
- 离线消息依赖中继留存策略（与现有同步一致）。
- 好友相关数据仅存本机并随加密快照同步；分享出去的作业副本归接收方本地所有。

---

## 模块三：Windows 桌面端增强（待 Windows 构建环境）

1. 托盘图标：最小化到托盘继续计时；托盘菜单（打开 / 开始番茄 / 退出）。
2. 系统级通知：Windows 通知中心推送作业截止与连胜提醒（比安卓本地通知更完整）。
3. 全局快捷键：`Ctrl+Shift+N` 快速录入作业小窗；`Ctrl+Shift+T` 开始/暂停番茄钟。
4. 拖拽导入 + 截图 OCR：拖入图片/PDF 识别作业（OCR 能力随桌面端插件接入）。
5. 桌面悬浮迷你窗（与模块四共享摘要组件）。

---

## 模块四：桌面组件（v0.34.0）

### 4.1 安卓桌面 App Widget

- 技术路线：`home_widget` 插件 + 原生 `AppWidgetProvider`（布局 XML + widget info XML）。
- 数据桥：App 每次 `persist()` 时把「桌面摘要」写入共享存储（今日待办数 / 连胜天数 / 今日 XP / 今日专注分钟 / 逾期数），widget 读取展示；数据变化主动刷新。
- 组件形态（2×2 / 4×1）：
  - 今日待办卡：剩余 N 项，点击打开 App
  - 连胜火焰 + XP 进度环
  - 番茄钟快捷按钮（一键开始专注）
  - 截止提醒（今天到期 / 已逾期红点）
- 支持多套组件大小与暗色模式适配。

### 4.2 Windows 悬浮迷你窗

- 无边框、置顶、可拖拽的小窗口（约 320×200），显示同一套摘要；常驻屏幕角落。
- 与安卓 Widget 共用「摘要数据 + 展示组件」逻辑（Flutter 侧一套代码）。

---

## 模块五：第二套界面风格（方案先行，暂不实施）

### 5.1 结论：风格切换"不重写"，建议做成可切换的第二套设计语言

- 现有架构已 Token 化：网页版颜色在 `theme.css` CSS 变量、组件样式在 `app.css`；安卓版颜色在 `SugarThemeData`、通用组件集中在 `widgets/basic.dart`。换风格主要改「主题层 + 通用组件层」，功能/数据/状态零改动。
- 不建议替换现有马卡龙风格，而是新增第二套风格作为可切换主题（设置页一键切换，随时切回）。

### 5.2 风格候选（待用户确认方向）

| 候选风格 | 特征 | 适配重点 |
| :--- | :--- | :--- |
| 简约线条风 | 白底 + 细线条图标、卡片去阴影改描边、更克制的圆角 | 图标 stroke 细化、阴影→边框、排版密度 |
| 玻璃拟态 | 毛玻璃卡片、弥散渐变背景、通透层次 | 背景渐变层、`backdrop-filter`、透明度体系 |
| 学习暗夜风 | 深色专注氛围、低饱和强调色、弱化装饰 | 暗色 Token 重做、对比度、专注场景联动 |

### 5.3 实施清单（确认风格方向后执行）

1. 新风格 Token 套（网页 `theme.css` 第二套变量 / 安卓 `SugarThemeData` 扩展）
2. 通用组件适配（卡片 / 按钮 / 开关 / 弹窗 / 导航 / 图标描边）
3. 重点页面微调（首页 / 专注 / 统计 / 设置）
4. 设置页新增「风格」切换入口
5. 两端同步验收 + 截图对比

---

## 版本规划（按《版本号更新体系》）

| 版本 | 内容 | 备注 |
| :--- | :--- | :--- |
| v0.32.0 | 模块一 激励体系 | 网页版 + 安卓版同步 |
| v0.33.0 | 模块二 好友直连 | 网页版 + 安卓版同步 |
| v0.34.0 | 模块四 桌面组件 | 安卓 Widget 先做；Windows 迷你窗随环境 |
| v0.35.0 起 | 模块三 / 模块五 | Windows 环境就绪 / 风格方向确认后立项 |

每个模块遵循：需求先行 → 两端同步开发 → 同步测试验收 → 同步版本日志 → 发布（tag + Actions 自动构建）。

---

## 验收要点

1. 激励体系：连胜/XP/徽章/欠账在断网状态下全部可用；数据随加密快照同步。
2. 好友直连：加好友 → 加密消息 → 共享作业 → 删除好友，全程无中心服务器；中继抓包只见密文。
3. 桌面组件：数据变化后 Widget 自动刷新；点击跳转 App 对应页面。
4. 风格切换：切换后全部页面无样式错乱，功能零改动，可一键还原。
5. 网页 e2e / 安卓测试分别新增对应用例；版本号两端一致。
