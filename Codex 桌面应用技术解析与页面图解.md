# Codex 桌面应用（Codex App）技术解析与页面图解

> 整理对象：OpenAI Codex 桌面应用（即本会话所在的应用）。  
> 资料来源：OpenAI 官方文档（App / Settings / Automations / Remote connections / Computer Use）、开源仓库 openai/codex、应用内实际观察。官方页面直连返回 403，以下基于官方搜索结果页内容与开源资料整理；凡官方文档未直接载明的内部实现（如 Electron / React / Rust 内核）均已标注来源。

---

## 1. 一句话定位

Codex App 是 OpenAI 出品的**面向智能体编码的桌面应用**：在本地 / SSH 远程项目上并行运行多条 Codex 线程（threads），内置 Git 工作树（worktree）、自动化任务（automations）、集成终端、Diff 审查与计算机使用（Computer Use）能力，官方定位是"管理多个编码代理的指挥中心"。

---

## 2. 技术栈

| 层 | 技术 | 说明 |
| :--- | :--- | :--- |
| 桌面壳 | Electron（Chromium + Node.js） | 应用外壳；Windows 通知等系统能力走 Electron |
| UI | React 19 + TypeScript | 侧边栏、对话、输入框、设置、面板均为 React 渲染 |
| 构建 | Vite / Rolldown | 前端打包与开发服务器（据开源仓库与社区分析） |
| 智能体内核 | Rust（codex-rs / app-server） | 开源仓库 openai/codex 的核心引擎，负责执行、沙箱、工具调用；桌面 UI 通过 app-server 接口与其通信 |
| 终端 | xterm 系集成终端 | 应用内终端（Ctrl+J），可运行命令、查看 agent 输出 |
| 远程 | SSH（Remote SSH） | 自动识别 SSH config 主机，远程项目上运行线程 |
| Git | 内置 Git / gh CLI 集成 | 提交、推送、创建 PR、worktree 隔离并行改动 |
| 桌面自动化 | 系统级（macOS/Windows） | 定时任务、自动化面板、Computer Use 插件 |

> 注：Electron / React / Rust 内核这一组合，官方文档未直接公开，依据开源仓库 openai/codex（app/ 为 Electron 目录、codex-rs 为 Rust 引擎）及公开报道整理；其余能力均有官方文档页面。

---

## 3. 主窗口整体布局

```text
┌─────────────────────────────────────────────────────────────────────┐
│ 顶栏  [项目▾] [模式▾] [模型▾]              [Actions] [账户] [⋯]      │
├──────────────┬────────────────────────────────────────┬─────────────┤
│ 左侧栏        │ 对话区（当前线程）                       │ 审查面板     │
│ ▶ Chat       │ ┌────────────────────────────────────┐ │ (可展开)    │
│   ▸ 项目A     │ │ 消息流：用户消息 / Agent 步骤 /      │ │ 文件 Diff   │
│     · 线程1   │ │ 工具调用 / 截图 / 图表 / 文件        │ │ 接受/拒绝   │
│     · 线程2   │ │                                    │ │ 行内评论    │
│   ▸ 项目B     │ └────────────────────────────────────┘ │            │
│ ▶ Projects  │  ┌ 输入框（Composer）───────────────┐  │            │
│ ▶ 自动化     │  │ [＋附件] [Plan↕] 输入指令… [⏎发送] │  │            │
│    · Triage │  └─────────────────────────────────┘  │            │
├──────────────┴────────────────────────────────────────┴─────────────┤
│ 内置终端（Ctrl+J）                              │ 状态栏 分支/token │
└─────────────────────────────────────────────────────────────────────┘
```

主线：**左侧选项目/线程 → 中间下指令 → 右侧看 Diff，底部终端看执行**。

---

## 4. 页面清单与图解

### 4.1 启动 / 登录页

```text
┌──────────────────────────────────────────────┐
│              Codex                           │
│                                              │
│        [🍬 OpenAI 账户登录 / 继续]            │
│                                              │
│  登录后：选择项目 或 打开本地文件夹            │
│  ▸ 最近项目                                    │
│    · D:\项目\GitHub项目\SugarPaper            │
│    · 其他仓库…                                │
│  ▸ 新建项目 / 打开文件夹 / SSH 远程主机        │
└──────────────────────────────────────────────┘
```

逻辑：首次使用需登录 OpenAI 账户；选择项目后进入主窗口；未登录或未选择项目时大部分功能不可用。

### 4.2 对话 / 线程页（核心页面）

```text
┌──────────────────────────────────────────────────────────┐
│ [◀ 返回项目]  项目名/分支  [Plan模式开关] [模型▾] [⋯]    │
├──────────────────────────────────────────────────────────┤
│ 用户消息                                                 │
│ ┌──────────────────────────────────────────┐            │
│ │ Agent 步骤：计划 → 工具调用 → 编辑文件     │            │
│ │   ✓ 读取文件 x.dart                      │            │
│ │   ✓ apply_patch 修改 3 个文件            │            │
│ │   ✓ 运行测试（输出摘要）                  │            │
│ └──────────────────────────────────────────┘            │
│ [Diff 链接] [截图/图表/文件渲染]                          │
├──────────────────────────────────────────────────────────┤
│ ┌ 输入框 ─────────────────────────────────┐              │
│ │ [＋ 附件] [Plan↕] 输入你的指令… [⏎ 发送] │              │
│ └─────────────────────────────────────────┘              │
└──────────────────────────────────────────────────────────┘
```

页面逻辑：

- 顶部：当前项目与分支、Plan 模式开关（Shift+Tab）、模型选择、更多操作（如查看记录、重命名、归档）。
- 消息流：用户消息与 Agent 消息交替；Agent 消息展示「规划 → 工具调用 → 改文件 → 验证」的可视化步骤，可展开查看详情。
- 富渲染：图片、视频、音频、图表（mermaid）、代码 diff 直接在对话内展示；文件以链接形式可点击打开。
- 输入框：多行输入、附件（图片等）、Plan 模式切换、发送；执行中可停止。
- 审批流：需要权限的操作（沙箱外、GUI、破坏性命令）会弹出确认，用户批准后才继续。
- 多代理：同一项目可开多条线程并行，侧边栏切换；子代理结果回传当前线程。

### 4.3 项目页 / 项目选择器

```text
┌──────────────────────────────────────────────────────────┐
│ Projects                          [新建项目] [打开文件夹]  │
├──────────────────────────────────────────────────────────┤
│ 本地项目                                                 │
│   ▸ SugarPaper（当前）            [＋新线程]              │
│     · 主分支 main                                       │
│     · worktree：codex/feature-x   ← Git 工作树（隔离改动）│
│   ▸ 其他仓库…                                            │
│                                                          │
│ 远程（SSH）                                              │
│   ▸ my-server（来自 ~/.ssh/config）                      │
│     [连接并在远程项目上运行线程]                          │
└──────────────────────────────────────────────────────────┘
```

逻辑：项目 = 一个本地文件夹 / Git 仓库 / SSH 远程目录；每个项目可建多条线程；worktree 让多个 agent 在同一仓库并行改不同分支而不冲突。

### 4.4 侧边栏（Chat / 自动化）

```text
┌──────────────────┐
│ ▶ Chat           │
│   ▸ 项目A         │
│     · 线程1 ●     │  ← 当前线程
│     · 线程2       │
│   ▸ 项目B         │
│     · 线程3       │
│   （可按时间倒序） │
│ ▶ Projects       │
│ ▶ 自动化          │
│    · Triage（收件箱）│
│    · 定时任务      │
│    · 线程自动化    │
└──────────────────┘
```

逻辑：线程默认按项目分组（也可切换时间视图）；Ctrl+B 折叠/展开侧边栏；自动化面板里 Triage 充当"收件箱"，定时任务的产出集中到这里。

### 4.5 内置终端

```text
┌──────────────────────────────────────────────────────────┐
│ [x] 终端（Ctrl+J 开关）      [新建终端] [清屏] [⋯]        │
├──────────────────────────────────────────────────────────┤
│ $ git status                                            │
│ $ flutter test ...  ✓ 全部通过                           │
│ $ codex "给某仓库加一个测试"   ← 可在终端里再跑一个 agent  │
└──────────────────────────────────────────────────────────┘
```

逻辑：应用内终端可执行任意命令、查看 agent 的 shell 输出（终端读取权限），也支持嵌套启动其它 agent；对 Git 不可信仓库会提示先处理 dubious ownership。

### 4.6 自动化 / Triage 面板

```text
┌──────────────────────────────────────────────────────────┐
│ 自动化                                    [＋新建任务]    │
├──────────────────────────────────────────────────────────┤
│ Triage（收件箱）                                         │
│   · 每日 CI 汇总报告             2小时前   [打开]        │
│   · 网站死链检查发现 3 处         昨天      [打开]        │
│                                                          │
│ 定时任务（Standalone）                                   │
│   · 每早 9:00 检查更新/汇总      cron 或预设周期          │
│                                                          │
│ 线程自动化（Thread）                                     │
│   · 每 5 分钟跟进当前线程任务    分钟级间隔               │
└──────────────────────────────────────────────────────────┘
```

逻辑：独立自动化按计划启动全新任务并把结果报告到 Triage；线程自动化在当前线程内按分钟级间隔跟进；可自定义 cron；无新内容时自动归档。

### 4.7 设置页

```text
┌──────────────────────────────────────────────────────────┐
│ 设置                                                     │
├──────────────────────────────────────────────────────────┤
│ 账户       登录状态 / 订阅 / 退出                         │
│ 外观       基础主题（浅/深）· 强调色 · 背景/前景色         │
│            界面字体 · 代码字体 · 分享/导入主题 · 宠物      │
│ 快捷键     查看/自定义（如切换模型、Plan、侧边栏）         │
│ 终端       字体、Shell、读取权限                          │
│ Git        默认分支前缀、提交/推送行为                    │
│ 通知       任务完成通知（macOS/Windows）                  │
│ Computer   安装插件 · 屏幕录制/辅助功能权限 ·             │
│ Use        锁定计算机时继续执行（Locked Computer Use）    │
│ 关于       版本号 / 更新 / 反馈                           │
└──────────────────────────────────────────────────────────┘
```

逻辑：外观支持完整自定义主题并可分享；Computer Use 需显式安装插件并授权系统权限；所有设置即时生效。

### 4.8 Diff / 审查面板

```text
┌────────────────────────────────────────────┐
│ 审查：3 个文件变更          [全部接受] [全部拒绝]│
├────────────────────────────────────────────┤
│ ▸ app/lib/main.dart  +12 -3                │
│ ▸ README.md          +45 -8                │
│ ▸ web/js/store.js    +1  -1                │
│ ┌──────────────────────────────────────┐   │
│ │ - const v = '0.31.0';                │   │
│ │ + const v = '0.32.0';   ← 行内评论   │   │
│ └──────────────────────────────────────┘   │
└────────────────────────────────────────────┘
```

逻辑：Agent 每次改动文件后生成 diff；用户可逐文件审查、接受/拒绝，或在代码行上挂行内评论反馈给 Agent。

---

## 5. 内置能力（与页面联动）

| 能力 | 入口 | 说明 |
| :--- | :--- | :--- |
| Skills | 对话中调用 | 加载技能包（SKILL.md）指导 Agent 执行专门任务 |
| Git 指令 | 对话后自动 | 提交、建分支、推送、创建 PR 等以指令形式回执在对话中 |
| 可视化 | 对话内渲染 | 图片/视频/音频/mermaid 图表直接显示 |
| 计划模式 | 输入框 Plan 开关（Shift+Tab） | 只读规划，产出计划供批准后再实施 |
| 多代理协作 | 线程内 | 可派生子代理并行处理子任务并回传结果 |
| Computer Use | 设置 → Computer Use | 授权后 Agent 可操作其它桌面应用 / 浏览器流程 |
| 远程 SSH | 项目页 | 在远程主机上运行线程 |

---

## 6. 页面风格提炼

1. **IDE 式三区布局**：左侧导航（项目/线程/自动化）+ 中央对话 + 右侧审查面板，底部终端 + 状态栏，信息密度高、专注任务流。
2. **极简顶部工具栏**：项目 / 模式 / 模型 / Actions 集中在顶栏，随时可切换；无多余装饰。
3. **消息即工作流**：对话不是纯聊天，而是「规划 → 工具调用 → 文件修改 → 验证」的结构化步骤卡片，可展开、可审批。
4. **强可视化**：diff 高亮、截图/图表内嵌、代码行内评论、文件链接，让 Agent 行为可审查。
5. **可深度定制外观**：基础主题 + 强调色 + 背景/前景 + 双字体（UI/代码）+ 主题导入分享。
6. **键盘优先**：Ctrl+J 终端、Ctrl+B 侧边栏、Shift+Tab Plan 等，桌面端快捷键密度高。

---

## 7. 官方参考

- [App – Codex（官方）](https://developers.openai.com/codex/app)：应用定位、并行线程、worktree、自动化、Git 功能
- [Settings – Codex app（官方）](https://developers.openai.com/codex/app/settings)：外观主题、字体、Computer Use
- [Automations – Codex app（官方）](https://developers.openai.com/codex/app/automations)：自动化面板、Triage、定时/线程自动化
- [Remote connections – Codex（官方）](https://developers.openai.com/codex/remote-connections)：SSH 远程项目
- [Computer Use – Codex app（官方）](https://developers.openai.com/codex/app/computer-use)：桌面应用操作能力
- 开源仓库：[openai/codex](https://github.com/openai/codex)（Rust 内核 codex-rs；app/ 为桌面端）

> 说明：官方页面本次直连返回 403，正文依据官方搜索结果片段 + 开源仓库 + 应用内实际界面整理；Electron/React/Rust 等内部实现细节以开源仓库与公开报道为准。
