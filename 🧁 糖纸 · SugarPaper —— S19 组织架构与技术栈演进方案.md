# 糖纸 · SugarPaper —— S19 组织架构与技术栈演进方案

> 版本：v0.4（原则已确认，待启动阶段 A）
> 日期：2026-08-09
> 状态：方向已由用户确认，等待启动指令

## 一、背景与目标

本方案确定三件事：

1. **各端选定专属技术栈**，互不套壳，每个平台使用最适合该平台的原生技术。
2. **全量重写**：不保留 Flutter 过渡版，按"设计系统 → 网页版 → 安卓 → Windows → 鸿蒙/小程序"顺序，各端从零重写，现有代码冻结归档、只作功能与契约的提取来源。
3. **界面风格整体升级**：参考 Codex、微信、ClassIsland 及 Apple HIG / Material 3 等成熟产品的视觉与交互，做一套现代化的设计系统，让所有端界面几乎一模一样、交互成熟。
4. **禁止套壳**：任何端不得用 Flutter 或其他跨端框架打包另一端的 UI。Flutter 仅作为当前过渡实现存在，重写完成后彻底移除，不再出现"一套代码多端打包"的形态。
5. **同形态界面必须一致（最高原则）**：按「手机形态 / 桌面形态」分两个视觉基准，与技术栈无关——
   - 手机形态：手机网页版、安卓版、鸿蒙手机版，界面必须一模一样（同一套布局、组件、操作逻辑）。
   - 桌面形态：电脑网页版、Windows 桌面版、鸿蒙电脑版，以及未来的 Linux / macOS 版，界面必须一模一样。
   - 手机形态与桌面形态之间允许布局差异（导航形态、信息密度、交互方式不同），但功能必须一致、设计语言必须统一。
   - 落地方式：手机形态以「手机网页版」为基准，桌面形态以「电脑网页版」为基准；各端用各自技术栈实现同一套设计系统与契约，视觉对拍验收。

重写期间保证：**用户已有数据不丢**（存储契约保持兼容，必要时提供迁移器）；**每次重写完成一个端即发版**；**功能以契约 + 一致性测试兜底**，不做"看起来像、行为不一样"的假对齐。

## 二、各端技术栈选型（确定版）

| 端 | 技术栈 | 视觉基准 | 选型理由 |
|---|---|---|---|
| 电脑网页版 + 手机网页版 | Vue 3 + TypeScript + Vite，自研组件库（不套第三方 UI 框架） | 设计系统 | 同一套代码响应式；TypeScript 为未来鸿蒙/小程序复用契约铺路；自研组件保证风格独特 |
| 安卓版 | Kotlin + Jetpack Compose + Material 3（自定义 ColorScheme 映射设计系统 Token）+ Room + DataStore + WorkManager | 手机网页版 | 官方推荐原生栈，性能与系统集成最佳，支持桌面小组件、分享、通知 |
| Windows 桌面版 | C# + .NET 8 + Avalonia + FluentAvalonia（ClassIsland 同款） | 电脑网页版 | 原生桌面质感：亚克力、毛玻璃、胶囊按钮、圆角卡片 |
| 鸿蒙版（未来） | ArkTS + ArkUI | 手机网页版 | HarmonyOS 官方原生栈 |
| 微信小程序（未来） | 微信原生小程序 + TypeScript（WXML/WXSS） | 手机网页版 | 小程序沙箱原生方案，无套壳 |
| iOS（未来可选） | Swift + SwiftUI | 手机网页版 | 苹果原生栈 |

技术栈共同约束：

- 每个端内部统一四层：表现层 → 应用层 → 领域层 → 平台层，依赖方向单向。
- 领域层实现同一份业务契约（见第六章），不依赖任何 UI 框架。
- 各端只消费设计系统（Token、组件规范、图标库），不跨端复制 UI 代码。
- **禁止套壳**：各端只使用上表专属技术栈独立开发；不得用 Flutter / uni-app / Taro 等跨端框架打包其他端的 UI。当前 `app/`（Flutter）为过渡实现，重写完成后拆分为 `app-android/`（Kotlin）与 `app-windows/`（C#）并移除 Flutter。

## 三、仓库组织（monorepo，各端自治）

```text
SugarPaper/
├── design-system/        # 设计系统：Token、组件规范、图标库 SVG、视觉基准图
├── contracts/            # 业务契约：数据 Schema、加密协议、计算规则、一致性测试向量
├── web/                  # 网页版（Vue 3 + TS，电脑 + 手机响应式）
├── web-app/              # ★ 网页版重写工程（Vue 3 + TS + Vite，开发期独立目录，完成后并入 web/）
├── app-android/          # 安卓原生（Kotlin + Compose）
├── app-windows/          # Windows 原生（C# + Avalonia）
├── app-harmony/          # 鸿蒙（ArkTS + ArkUI，未来）
├── app-miniprogram/      # 微信小程序（未来）
├── app-ios/              # iOS（Swift + SwiftUI，未来可选）
├── site/                 # 官网（GitHub Pages）
├── docs-src/             # 文档站源码（VuePress）
├── updates/              # 更新清单与各平台安装包元数据
├── docs/                 # 产品文档统一归档
└── scripts/              # 版本一致性校验、对拍测试、视觉对比脚本
```

旧工程处置：

- 现有 `app/`（Flutter）与 `web/`（旧原生 JS）在各自重写完成后冻结归档（保留在 git 历史与 `archive/` 参考目录中，只读不维护）。
- 现有 `homepage/`（旧 Vue3 官网）由 `site/` 取代后归档。
- 根目录 `*.md` 文档收拢进 `docs/`（移动前列清单确认）。

## 四、界面风格方向（参考 Codex、微信等）

### 4.1 总体设计语言

"现代、克制、有层次"的效率工具风格，融合四类参考：

| 参考对象 | 借鉴点 |
|---|---|
| 微信 | 信息架构与移动端布局：底部五栏导航（首页/日历/专注/统计/我的）、列表+详情两级结构、好友直连用消息气泡、通知角标 |
| Codex（桌面 App） | 桌面端质感：左侧导航栏 + 主内容区 + 底部输入区；克制的中性色 + 单一强调色；清晰的层级（背景/卡片/悬浮层）；流畅的列表与过渡动画；不花哨 |
| ClassIsland / 现代桌面软件 | 亚克力毛玻璃卡片、胶囊按钮、渐变高光、柔和阴影、跟随系统主题 |
| Apple HIG / Material 3 | 动效时长与缓动曲线、触控目标尺寸、深色模式、无障碍对比度、语义层级 |

### 4.2 视觉要素

- **色彩**：中性底色（浅色/深色）为主，配合少量强调色；糖纸的暖色品牌色作为主题可选项，一套 Token 支持多主题切换，图标颜色跟随主题。
- **卡片**：圆角卡片 + 细描边 + 柔和阴影；桌面端可选毛玻璃（背景模糊）。
- **按钮**：主按钮 = 渐变高光 + 胶囊圆角；次按钮 = 描边；危险操作 = 明确红色，二次确认。
- **图标**：一套统一风格线性 SVG 图标库（无 emoji），各端导入为原生资源。
- **字体**：系统字体栈（各端原生），数字使用等宽字形便于统计对齐。
- **动效**：150–300ms、标准缓动；页面切换过渡、列表项出现、开关、展开收起均有动效，不炫技。
- **深浅色**：跟随系统 + 手动切换，两端基准一致。

### 4.3 布局规范

- 手机端（安卓/鸿蒙/小程序/手机网页版）：底部导航五栏 + 顶部标题栏 + 内容列表；支持下拉刷新、滑动操作、返回手势。
- 电脑端（Windows/电脑网页版宽屏）：左侧导航栏（可折叠）+ 主内容区 + 侧边详情/统计面板；支持键盘快捷键、悬停态、拖拽。
- 桌面窗口（Windows）：可拖拽、可缩放、最小化到托盘、系统通知。

## 五、交互成熟化标准（每端必须达到）

| 类别 | 要求 |
|---|---|
| 全局反馈 | 加载骨架屏、空状态引导（含首次使用教程）、错误提示（toast/snackbar）、操作成功反馈 |
| 列表操作 | 手机端滑动操作（完成/删除/收藏）、长按菜单、多选批量；桌面端右键菜单、悬停操作按钮 |
| 表单 | 即时校验、错误提示、防误触、自动保存草稿 |
| 导航 | 返回栈、页面状态恢复、深链接（网页/安卓）、键盘快捷键（桌面） |
| 数据 | 离线优先、本地即时读写、同步冲突友好提示、自动保存 |
| 撤销与保护 | 删除可撤销（临时回收站或确认弹窗）、危险操作二次确认 |
| 无障碍 | 对比度达标、字体缩放跟随系统、TalkBack / 旁白语义标签、触控目标不小于 48dp |
| 通知 | 作业到期提醒、专注结束提醒、好友消息通知，各端使用系统通知 |
| 空态与新手引导 | 首次启动用户协议（已有）、空列表引导创建、功能引导浮层 |

## 六、功能对齐的保障：契约 + 一致性测试

各端语言不同，不共享代码，共享**契约**：

| 契约 | 内容 | 对齐方式 |
|---|---|---|
| 数据模型 Schema | 作业、科目、统计、好友、激励等数据结构 | 一份权威 JSON Schema，各端实现解析与校验 |
| 加密协议 | AES-256-GCM 信封、公钥派生、消息编码 | 同一输入，各端输出必须一致 |
| 计算规则 | XP、连胜、徽章、欠账双倍、统计指标 | 同一输入，各端结果必须一致 |
| 存储结构 | 键名、表结构、文件格式、默认值 | 与旧版兼容，保证用户数据迁移无损 |
| 同步协议 | Nostr kind、消息类型、好友直连流程 | 同一协议实现 |

- 测试向量放 `contracts/tests/`，各端用原生测试框架跑同一套（Vitest / Kotlin Test / xUnit / 小程序测试工具）。
- **任何端上线前必须全部通过**；新端（鸿蒙、小程序）接入 = 实现契约 + 过测试 + 过视觉对拍。

## 七、版本管理与发布

- 产品版本号全局统一（X.Y.Z），所有端同步发布同一版本号；每次发版必须写日志。
- 全量重写按阶段发版：每重写完成一个端即升一个版本号；全部端完成之日发布 v1.0.0。
- 各端独立构建：安卓 APK（按 ABI 分包）、Windows 安装包、Web/官网静态站、小程序包、鸿蒙 HAP。
- `updates/latest.json` 按端与 ABI 记录下载地址与 SHA-256。
- `scripts/check_version.js` 校验各端/updates/README 版本一致，不一致 CI 报错。
- 发布由用户手动打 tag 触发。

## 八、全量重写路线图

顺序原则：先立地基（设计系统 + 契约）→ 先重写网页版（最快的发布渠道，也是视觉基准）→ 再移动端与桌面端。

| 阶段 | 内容 | 版本示例 |
|---|---|---|
| A 地基 | 建 `design-system/`（Token、组件规范、图标库、基准图）与 `contracts/`（Schema、协议、测试向量）+ 版本校验脚本；从旧代码提取全部功能清单与数据格式 | v0.34.0 |
| B 网页版重写 | Vue 3 + TS 全量重写（电脑 + 手机响应式），逐模块对照旧版功能清单实现；成为视觉与功能基准 | v0.35.0 |
| C 安卓重写 | Kotlin + Compose 全量重写，界面照手机网页版，逐模块对拍 | v0.36.0 |
| D Windows 重写 | C# + Avalonia 全量重写（ClassIsland 质感），界面照电脑网页版 | v0.37.0 |
| E 鸿蒙 / 小程序立项 | 按契约 + 视觉对拍接入 | 视规划 |
| F 收尾 | 全部端达到功能对拍后，旧 Flutter 端下线归档，发布 v1.0.0 | v1.0.0 |

重写模块优先级（每端内部按此顺序交付）：

1. 账号与首启协议（用户协议/隐私政策、主题选择）
2. 作业管理（科目、添加、编辑、完成、搜索、筛选）
3. 首页概览（今日任务、进度、统计摘要）
4. 日历（月视图、规划未来作业）
5. 专注（番茄钟 + 白噪音）
6. 统计与激励（XP、连胜、徽章）
7. 同步与好友直连
8. 教师模式、家庭档案、备注、提醒等进阶功能

数据兼容承诺：

- 网页版重写后继续读写同一批 storage key（契约保证），用户数据零迁移。
- 安卓重写后提供 SQLite 迁移器，从旧 Flutter 数据文件一键导入。

## 九、需要你拍板的决策点

1. **界面风格方向**：确认按"微信式信息架构 + Codex 式桌面质感 + ClassIsland 毛玻璃/胶囊 + HIG/Material 动效与无障碍"执行？还是你稍后发来的界面参考文档优先？
2. **网页版是否引入 Vue 3 + TS**：重写即新工程，建议直接用（比原生 JS 更利于长期维护与鸿蒙/小程序契约复用）；如你更想保持原生 JS 也可以，但不利于后续复用。
3. **版本节奏**：按"每端重写完成升一个大版本，全部完成发 v1.0.0"执行，还是全程小版本递增？
4. **旧代码归档**：确认 Flutter 与旧 web 重写完成后只读归档（保留在 git 历史），不再发布更新。
5. **数据兼容**：确认"网页版 storage 键保持不变 + 安卓提供迁移器"的方案。

## 十、风险与规避

| 风险 | 规避方式 |
|---|---|
| 全量重写工作量大、周期长 | 分端分阶段发版；每端按模块优先级交付；功能清单先行 |
| 重写期间无新版可发布 | 网页版先重写先发布，其他端暂用旧版，避免全平台空窗 |
| 用户数据丢失 | 存储契约兼容 + 迁移器 + 上线前迁移演练 |
| 各端界面跑偏 | 视觉基准图 + 视觉对拍 + 平台适配规则 |
| 功能遗漏 | 从旧代码提取完整功能清单，逐项勾销 |
