# 糖纸 · SugarPaper —— S19 任务交互优化方案

> 版本：v0.1（草案，待审核）
> 日期：2026-08-09
> 状态：待用户审核后实施

## 一、目标

把任务管理的交互从"能用"提升到"好用、成熟"，参考国内大厂与成熟效率产品（微信、支付宝、滴答清单、Things）的交互标准，随各端重写一并落地。

## 二、交互总原则

1. **少打扰**：默认不弹窗打扰，操作有反馈但不打断。
2. **可撤销**：删除、完成等关键操作可撤销（5 秒窗口）。
3. **状态清晰**：任何任务一眼可知：待办 / 进行中 / 已完成 / 已逾期。
4. **单手可达**（手机）：高频操作在拇指热区，底部导航 + 底部弹层。
5. **键盘友好**（桌面/网页）：快捷键全覆盖，可完全键盘操作。
6. **零学习成本**：首次使用有引导，空状态有创建入口。

## 三、任务全生命周期交互

### 3.1 创建

- 顶部常驻快速添加框：点击聚焦即输入，回车创建（桌面支持快捷键）。
- 粘贴自动解析（已有）：URL、日期、科目自动识别，重写后保留并强化。
- 语音输入（已有）：保留，增加方言友好提示。
- 自然语言日期：输入"明天""周五""下周一"自动识别截止时间。
- 连续录入模式：创建一条后输入框保持聚焦，可连续快速录入。
- 科目选择：输入时联想，或点击科目色块快速切换（颜色按色相有序）。

### 3.2 列表与分组

- 默认分组：今天 / 明天 / 本周 / 以后 / 长期 / 已完成。
- 排序：按截止时间（默认）、优先级、科目、手动拖拽（桌面端）。
- 逾期任务：单独置顶分组，红色时间标识，提供"移到今天"一键操作。
- 筛选：科目、状态（待办/完成/逾期）、搜索关键字。
- 分组折叠：可折叠收起，记忆展开状态。

### 3.3 列表项操作

- 手机端：
  - 左滑/右滑：完成、稍后提醒、删除（可配置顺序）。
  - 长按：多选模式，批量完成 / 删除 / 改科目 / 改日期。
  - 点击：打开详情（底部弹层）。
- 桌面/网页端：
  - 悬停：显示操作按钮（完成、删除）。
  - 右键：上下文菜单（完成、稍后、编辑、删除）。
  - 拖拽：排序、拖入"今天/以后"分组。
  - 多选：勾选 + Shift 连选。

### 3.4 详情（底部弹层 / 侧边面板）

- 字段：科目（色块标识）、截止时间、优先级、备注、子任务、标签、提醒。
- 状态流转：待办 → 进行中 → 已完成；逾期可"改为今天"。
- 快速编辑：点击字段即改，自动保存（不设保存按钮）。
- 删除：二次确认（含"同时删除子任务"提示），删除后 toast 可撤销。

### 3.5 完成与撤销

- 完成动画：任务划掉 + 微收缩 + 移入"已完成"（150–250ms）。
- 完成即时反馈：toast"已完成"，5 秒内可撤销。
- 完成计入当日 XP 与统计（沿用激励体系）。

### 3.6 提醒

- 截止前 24 小时 / 1 小时系统通知（可配置）。
- 列表项"稍后提醒"：15 分钟 / 1 小时 / 明天。
- 专注结束通知（已有）。
- 好友消息通知（已有）。

### 3.7 空状态与引导

- 空列表：插图 + 一句话引导 + "添加第一条作业"按钮。
- 首次使用：3–5 步引导（添加作业 → 完成 → 查看统计），可跳过。

## 四、各端快捷键 / 手势对照

| 操作 | 手机端 | 桌面/网页端 |
|---|---|---|
| 快速添加 | 底部导航中间"+"或顶部输入框 | Ctrl/Cmd + N |
| 完成 | 滑动 / 点击左侧圆 | 悬停点击 / Ctrl + Enter |
| 搜索 | 顶部搜索图标 | Ctrl/Cmd + K |
| 多选 | 长按 | Ctrl/Cmd + 点击 / Shift 连选 |
| 删除 | 滑动 / 详情内删除 | Delete 键（有撤销） |
| 撤销 | toast 内按钮 | Ctrl/Cmd + Z |
| 专注 | 底部导航 | Ctrl/Cmd + F |

## 五、与设计系统 / 契约的关系

- 交互规范（状态、动效时长、手势、文案）写入 `design-system/`，与组件规范同级，各端实现同一套。
- 动效统一：150–300ms、标准缓动、尊重系统"减弱动态效果"设置。
- 无障碍：48dp 触控目标、对比度达标、TalkBack/旁白语义、字体缩放跟随系统。

## 六、实施顺序

随 S19 全量重写落地：

1. 网页版重写（v0.35.0）先实现全部交互规范，成为基准。
2. 安卓重写（v0.36.0）逐条对照实现。
3. Windows 重写（v0.37.0）实现桌面专属交互（右键、拖拽、快捷键）。
4. 交互规范进 CI：视觉对拍 + 交互用例清单勾销。

