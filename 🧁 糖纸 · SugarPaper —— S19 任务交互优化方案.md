# 糖纸 · SugarPaper —— S19 任务交互优化方案

> 版本：v0.2（草案，待审核）
> 日期：2026-08-09
> 状态：待用户审核后实施

## 一、目标

把任务管理的交互从"能用"提升到"好用、成熟"，布局与信息架构参考 **Microsoft To Do** 与 **Sticky Notes（微软便签）**，核心原则是**渐进式展示**：界面不把功能全铺出来，主页只露核心，深层功能收进二级页面，通过明确的跳转入口进入。

## 二、参考产品布局分析

### 2.1 Microsoft To Do

| 特点 | 说明 | 糖纸借鉴点 |
|---|---|---|
| 三级结构 | 左侧导航 → 中间列表 → 右侧详情，宽屏三栏 | 桌面端采用"导航/列表/详情"布局，手机端用"列表 → 全屏详情"栈 |
| 智能列表 | 「我的日子 / 重要 / 任务 / 计划」自动分组 | 首页保留「今天 / 明天 / 本周 / 长期 / 已逾期」自动分组 |
| 侧边栏可折叠 | 收起后只留图标，给内容更多空间 | 桌面端左侧导航可折叠 |
| 顶部常驻添加框 | 列表顶部随时输入新建任务 | 首页顶部常驻快速添加框 |
| 详情面板分组 | 步骤 / 备注 / 提醒 / 重复 / 截止 / 文件分区块 | 作业详情面板分区块：科目 / 截止 / 优先级 / 备注 / 图片 / 提醒 |
| 完成交互 | 勾选划线 + 移入「已完成」 | 沿用并加 5 秒撤销 |
| 克制 | 列表行只显示标题 + 日期，不堆功能按钮 | 列表行默认只露 1–2 个操作，其余收进详情与右键/长按菜单 |

### 2.2 Sticky Notes（微软便签）

| 特点 | 说明 | 糖纸借鉴点 |
|---|---|---|
| 极简卡片 | 列表只显示便签标题/摘要，点击进入全屏编辑 | 便签列表卡片化，点卡片进全屏编辑 |
| 全局快捷键 | 一键新建便签 | 桌面端 Ctrl/Cmd + N 新建作业、新建便签 |
| 不打扰 | 无多余 UI，专注记录 | 编辑界面只留必要操作，次要操作收进「更多」 |
| 颜色/置顶 | 颜色区分、置顶标识 | 便签保留颜色与置顶，列表上用小标识而非展开全部功能 |

## 三、渐进式展示原则（界面逻辑核心）

1. **主页只露核心**：每个页面默认展示最重要的 2–5 项，不把所有功能平铺。
2. **二级页面承接深层功能**：次要功能通过「查看全部 / 更多 / 进入」跳转到独立页面，而不是堆在当前页。
3. **列表行克制**：行内默认只显示核心信息与 1–2 个高频操作，其余收进详情、右键/长按菜单。
4. **分组可折叠**：长列表按组折叠，默认收起低频组（如已完成、归档）。
5. **轻操作弹层、重操作全屏**：简单编辑用弹层；复杂编辑（多字段、图片、子任务）用全屏页。
6. **更多按钮收纳**：次要操作统一收进「…」菜单，避免按钮排满界面。

## 四、各页面界面逻辑优化清单

### 4.1 首页（今日任务）

- 概览卡只显示 3 项核心：今日进度环、连胜、今日 XP；**点击卡片进入统计页**，不展开详细图表。
- 任务列表分组：今天 / 明天 / 本周 / 长期 / 已逾期，组头可折叠。
- 已完成任务默认折叠，展开后显示「查看全部已完成」跳转到任务归档页。
- 顶部常驻快速添加框；行内默认只露完成圆框与截止日期，更多操作收进长按/右键。

### 4.2 成就徽章（用户示例，落地为规范）

- 设置页/我的页只露出**最近解锁 2–3 枚徽章** + 「查看全部」入口。
- 点击进入**成就徽章墙独立页**：全部 12 枚、已解锁/未解锁、解锁条件、进度提示。
- 未解锁徽章用轮廓/剪影表示，不展开全部说明，点击单个徽章才看条件详情。

### 4.3 专注页

- 场景卡片默认只显示常用 3–4 个 + 「更多场景」入口进入场景库页面（全部场景、自定义声音管理）。
- 番茄钟设置（时长、提醒、呼吸引导）收进右上角设置按钮，弹出设置层，不铺在主界面。
- 今日专注统计只显示一行核心数字，点击进入专注统计详情。

### 4.4 统计页

- 默认只显示核心图表：进度环 + 每日趋势柱状图。
- 饼图、折线图、欠账排行收进「详细报告」二级页或折叠区。
- 时间范围切换保留在页头，不新增冗余选项。

### 4.5 我的页（设置）

- 按卡片分组：账号与数据 / 激励与成就 / 主题与外观 / 通知提醒 / 法律与隐私。
- 每组只显示关键摘要（如账号短 ID、主题名、徽章数），**点击进入各自的二级页面**，不把设置项全铺在列表里。
- 科目管理、教师模式、家庭模式等均为独立入口，点击进入独立页。

### 4.6 便签（Sticky Notes 风格）

- 列表卡片只显示：标题、摘要一行、颜色标识、置顶星标；点击进入全屏编辑。
- 新建用悬浮按钮 + 快捷键；编辑页只留必要操作，其余收进「…」。

### 4.7 作业详情

- 详情分区块展示（参考 To Do 右侧面板）：科目 / 截止 / 优先级 / 备注 / 图片 / 子任务 / 提醒。
- 区块默认折叠次要项（子任务、提醒），点击展开。
- 删除等危险操作收进「…」菜单并二次确认。

## 五、各端快捷键 / 手势对照

| 操作 | 手机端 | 桌面/网页端 |
|---|---|---|
| 快速添加 | 底部导航中间"+"或顶部输入框 | Ctrl/Cmd + N |
| 完成 | 滑动 / 点击左侧圆 | 悬停点击 / Ctrl + Enter |
| 搜索 | 顶部搜索图标 | Ctrl/Cmd + K |
| 多选 | 长按 | Ctrl/Cmd + 点击 / Shift 连选 |
| 删除 | 滑动 / 详情内菜单 | Delete 键（有撤销） |
| 撤销 | toast 内按钮 | Ctrl/Cmd + Z |
| 新建便签 | 悬浮按钮 | Ctrl/Cmd + Shift + N |

## 六、与设计系统 / 契约的关系

- 交互规范（信息架构、渐进式展示规则、状态、动效时长、手势、文案）写入 `design-system/`，与组件规范同级，各端实现同一套。
- 动效统一：150–300ms、标准缓动、尊重系统"减弱动态效果"设置。
- 无障碍：48dp 触控目标、对比度达标、TalkBack/旁白语义、字体缩放跟随系统。

## 七、实施顺序

随 S19 全量重写落地：

1. 网页版重写（Vue 3 + TS）先实现全部交互规范，成为基准。
2. 安卓重写（Kotlin + Compose）逐条对照实现。
3. Windows 重写（C# + Avalonia）实现桌面专属交互（三栏布局、右键、拖拽、快捷键）。
4. 交互规范进 CI：视觉对拍 + 交互用例清单勾销。

