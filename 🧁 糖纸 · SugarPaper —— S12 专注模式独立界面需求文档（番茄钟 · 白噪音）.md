# 🧁 糖纸 · SugarPaper —— S12 专注模式独立界面需求文档（番茄钟 · 白噪音）

> 版本：v1.0（2026-08-07） · 对应里程碑：S12 → v0.26.0
> 适用平台：**网页版（web/）与安卓版（app/）同步开发、同步交付、同步发布**，本文档为两平台共同需求基准。

---

## 1. 背景与目标

### 1.1 现状与问题

糖纸目前已有完整的番茄钟 + 白噪音专注能力（S1/S3/S4 引入），但存在两个体验问题：

1. **入口不独立**：专注功能散落在「任务卡 → 专注」和设置页中，没有自己的一级页面；用户想开番茄钟要先找到任务或翻设置。
2. **界面信息密度低**：专注场景（环境/噪音）选择、音量、混音、呼吸引导等控件挤在一起，缺少像主流番茄钟产品（潮汐 / Forest / 滴答清单等）那样「一屏一场景、环境氛围感强」的体验。

### 1.2 目标

- 为番茄钟与白噪音建立**独立的一级页面（Tab）**，网页版与安卓版各加一个「专注」入口。
- 界面参考主流番茄钟 + 白噪音产品：**中央计时环 + 环境氛围背景 + 场景卡片选择 + 独立音量/混音控制**。
- 番茄钟与白噪音**同一页面内协同**：选好环境与噪音后一键开始专注。
- 底部导航由 5 项调整为 6 项，专注置于中间；各 Tab 使用统一的「图标 + 文字」样式（v0.26.0 起专注不再做圆形突出按钮，与其它 Tab 完全一致），保证 6 项不拥挤、多端视觉统一。
- 多版本（网页版 / 安卓版 / 后续 Windows / iOS / macOS / Linux / 鸿蒙）从本需求起**必须同步开发、同步版本、同步文档**。

---

## 2. 界面设计

### 2.1 整体布局（自上而下）

```text
┌────────────────────────────────┐
│ 顶部：返回/关闭（任务联动时）· 今日专注统计       │
│ 背景：专注氛围渐变（随所选场景切换）             │
│ ┌ 卡片① 番茄钟 ─────────────────────────┐ │
│ │ 圆形计时环（SVG 描边）· 模式标签 · 轮次      │ │
│ │ 预设：番茄/短休/长休/自由 · 开始/暂停/跳过/结束 │ │
│ │ 当前场景 + 主音量 + 呼吸引导               │ │
│ └──────────────────────────────────────┘ │
│ ┌ 卡片② 环境与噪音 ─────────────────────┐ │
│ │ 场景卡片网格（9 种 + 静音 + 自定义，渐变氛围）│ │
│ └──────────────────────────────────────┘ │
│ ┌ 卡片③ 声音与氛围 ─────────────────────┐ │
│ │ 呼吸引导开关 · 叠加场景 + 叠加音量          │ │
│ │ 自定义声音：上传 ≤2MB / 更换 / 删除        │ │
│ └──────────────────────────────────────┘ │
└────────────────────────────────┘
```

> 视觉规范：全部使用自绘 24×24 SVG 描边图标（禁止 emoji）；卡片圆角 20–22px、柔和阴影、马卡龙渐变底色，与糖纸其余界面保持一致。

### 2.2 底部导航（6 项）

顺序：**首页 · 便签 · 专注（居中）· 日历 · 统计 · 我的**

- 「专注」为一级 Tab，与其它 Tab 使用**统一的「图标 + 文字」样式**（同一套 SVG 图标、同尺寸、激活同色高亮），不设特殊圆形按钮，保证网页版 / 安卓版多端一致。
- 网页版：底部导航 6 项；桌面宽屏下侧边栏同步加入「专注」入口。
- 安卓版：底部导航 6 项；专注 Tab 与其它 Tab 样式统一（无特殊放大 / 圆形按钮）。

### 2.3 环境与噪音选择

- 场景卡片网格：每张卡片包含 **渐变氛围底色 + 图标 + 名称**，点击即切换环境并开始播放该噪音。
- 内置 9 种场景：雨声 / 海浪 / 森林 / 咖啡馆 / 篝火 / 粉红噪音 / 棕噪音 / 图书馆 / 夜虫（安卓纯 Dart 合成 WAV；网页 Web Audio 实时合成）。
- 选中场景后，页面背景氛围色随场景渐变（`--scene-a / --scene-b`），形成「环境感」。
- 静音（none）与自定义声音为独立卡片。

### 2.4 番茄钟

- 四种预设：番茄 25 / 短休 5 / 长休 15 / 自由专注（不限时，仅记录）。
- 圆形进度环：剩余时间随进度描边；完成后撒糖果 + 提示音（可关）。
- 番茄轮次：每完成 1 轮记录 1 颗番茄，4 轮后进入长休。
- 任务联动：从任务卡进入时自动关联任务；专注页内可显示关联任务科目与标题。
- 数据记录：完成即写入 `focusSessions`（时长 / 场景 / 任务），供日历角标与统计使用。

### 2.5 白噪音与混音

- 主场景音量独立滑块（0–100%）。
- 叠加音（Noisli 式混音）：可选任意可用场景或自定义声音作为叠加，独立音量滑块。
- 自定义声音：上传本地音频 ≤2MB（网页 dataURL / 安卓 dataURL 或文件），可作主场景或叠加场景，可删除。
- 所有选择（场景 / 音量 / 叠加 / 自定义声音）持久化到 `settings.focus`，重新进入页面自动恢复。

### 2.6 今日专注统计

- 页面顶部展示：今日已专注分钟数 + 今日番茄数。
- 数据来源：`focusSessions`（当日 `startedAt`/`endAt` 命中）。

---

## 3. 功能需求清单

| 编号 | 功能 | 网页版 | 安卓版 |
| :--- | :--- | :---: | :---: |
| F1 | 「专注」独立一级 Tab（底部导航第 3 项，与其它 Tab 样式统一） | ✔ | ✔ |
| F2 | 中央圆形计时环 + 模式标签 + 轮次 | ✔ | ✔ |
| F3 | 番茄/短休/长休/自由 四档预设 | ✔ | ✔ |
| F4 | 开始/暂停/跳过/结束 | ✔ | ✔ |
| F5 | 9 种内置白噪音场景卡片（渐变氛围） | ✔ | ✔ |
| F6 | 场景选择即切换环境背景 | ✔ | ✔ |
| F7 | 主场景音量滑块 | ✔ | ✔ |
| F8 | 叠加场景选择 + 叠加音量 | ✔ | ✔ |
| F9 | 自定义声音上传（≤2MB）/ 删除 | ✔ | ✔ |
| F10 | 呼吸引导开关 | ✔ | ✔ |
| F11 | 任务联动（任务卡进入专注） | ✔ | ✔ |
| F12 | 今日专注分钟/番茄统计 | ✔ | ✔ |
| F13 | 专注会话持久化 + 日历角标 + 统计联动 | ✔ | ✔ |
| F14 | 设置持久化（settings.focus） | ✔ | ✔ |

---

## 4. 多版本开发同步规范（自本需求起强制）

> 本规范用于保证未来所有平台版本（网页版、安卓版、Windows、iOS、macOS、Linux、鸿蒙）不再出现功能与版本错位。

### 4.1 同步开发

- 新功能（S 里程碑）必须先产出**本文件形式的需求文档**，再同时立项网页版与安卓版开发。
- 两平台同一里程碑**并行开发、同一轮验收**，禁止一平台领先多个版本后才回补。

### 4.2 同步版本号

- 功能对齐的版本号两平台**完全一致**（沿用 README「安卓版与网页版功能对齐时沿用同版本号」约定）。
- 安卓版以 `pubspec.yaml` 的 `version` 为唯一事实来源；网页版以 README 版本更新日志为基准。
- 发布时必须三处一致：README / 代码内版本号 / 安装包文件名。

### 4.3 同步文档

- README 采用「单一 README · 多版本分区管理」：每完成一个里程碑，两平台各自的开发日志与 `#### vX.Y.Z` 更新日志**同时**补齐。
- 需求文档放仓库根目录，命名带里程碑编号（如 `S12 ... 需求文档`），README「文档」章节登记链接。
- 平台差异（如 PWA 离线 vs 原生离线、拖拽 vs 上下移动）必须在 README 中**显式标注为平台适配**，不得算作功能缺口。

### 4.4 同步验收

- 单元测试 / 端到端测试两平台各自维护；新功能必须有对应测试用例。
- 发布前检查清单：功能清单逐项核对（见第 3 节）、版本号三处一致、README 日志已写、APK 已构建验证。

---

## 5. 验收标准

1. 网页版与安卓版均可从底部导航一键进入「专注」页，界面与交互一致。
2. 选择任一场景后：环境背景渐变切换 + 噪音开始播放 + 主音量可调；叠加场景与音量独立生效。
3. 番茄钟计时、暂停、跳过、结束、轮次、撒糖果全部可用；完成后 `focusSessions` 正确落库。
4. 自定义声音上传 ≤2MB 生效，可作主场景/叠加场景，可删除；上传后重进页面仍保留。
5. 今日专注统计与日历专注角标、统计页专注数据一致。
6. 底部导航 6 项无溢出、无错位；深色模式与七套主题下视觉正常。
7. 两平台版本号一致（v0.26.0），README 两平台更新日志均已补齐。

---

## 6. 版本规划

| 里程碑 | 版本 | 内容 | 状态 |
| :--- | :--- | :--- | :--- |
| S12 | v0.26.0 | 专注模式独立界面（番茄钟 + 白噪音）+ 底部导航 6 项重构 | 📦 本次交付 |
